{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run llmgate",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "cwd": "${workspaceFolder}",
      "program": "${workspaceFolder}/cmd/llmgate",
      "args": ["-config", "config.json"]
    }
  ]
}
