{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run aigate",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "cwd": "${workspaceFolder}",
      "program": "${workspaceFolder}/cmd/aigate",
      "args": ["-config", "config.json"]
    }
  ]
}
